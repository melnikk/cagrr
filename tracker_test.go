package cagrr_test

/*
import (
	. "github.com/skbkontur/cagrr"

	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Table", func() {
		var (
			table   Table
			repairs map[int]*Repair
		)

		BeforeEach(func() {
			table = Table{}
			repairs = RepairMap{
				1: nil,
				2: nil,
				3: nil,
				4: nil,
				5: nil,
			}
			table.Repair(repairs)
		})

		Context("Percentage", func() {
			It("should return zero percent when created", func() {
				Expect(table.Percentage()).To(BeNumerically("==", 0))
			})

			It("should increment percent when fragment complete", func() {
				table.CompleteFragment(1, 0)
				Expect(table.Percentage()).To(BeNumerically("==", 20))
			})
		})
})

*/
